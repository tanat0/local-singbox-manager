[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
# run only unit tests by default; for e2e: pytest tests/e2e --browser chromium
addopts = --ignore=tests/e2e
