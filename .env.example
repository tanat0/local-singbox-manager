# === Sing-Box Manager environment ===
# Copy to .env and adjust if needed (all values have sane defaults).

# Bind address — do NOT change to 0.0.0.0 (LAN exposure)
HOST=127.0.0.1
PORT=9090

# Paths
SINGBOX_BIN=/usr/bin/sing-box
HELPER_BIN=/usr/local/bin/singbox-manager-helper
DATABASE_URL=sqlite:///./singbox_manager.db
