# /etc/sudoers.d/singbox-manager
#
# Allows the sing-box manager web app (running as __SBM_USER__) to call
# the privileged helper without a password prompt.
#
# This template is rendered by scripts/install-systemd.sh.

__SBM_USER__ ALL=(root) NOPASSWD: __SBM_HELPER_BIN__
